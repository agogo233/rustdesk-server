apiVersion: apps/v1
kind: Deployment
metadata:
  name: rustdesk-server
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: rustdesk
  template:
    metadata:
      labels:
        app: rustdesk
    spec:
      containers:
        # id Server (HBBS)
        - name: hbbs
          image: rustdesk/rustdesk-server:latest
          env:
            - name: RELAY_HOST
              value: "rustdesk.yourdomain.tld"
          command: ["hbbs", "-r", "$(RELAY_HOST):31117", "-k", "_"]
          volumeMounts:
            - mountPath: /root
              name: rustdesk-data
          ports:
            - containerPort: 31115
            - containerPort: 31116
              protocol: TCP
            - containerPort: 31116
              protocol: UDP
            - containerPort: 31118

        # Relay Server (HBBR)
        - name: hbbr
          image: rustdesk/rustdesk-server:latest
          command: ["hbbr", "-k", "_"]
          volumeMounts:
            - mountPath: /root
              name: rustdesk-data
          ports:
            - containerPort: 31117
            - containerPort: 31119

      volumes:
        - name: rustdesk-data
          persistentVolumeClaim:
            claimName: rustdesk-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: rustdesk-service
spec:
  type: LoadBalancer # Or NodePort
  selector:
    app: rustdesk
  ports:
    - name: hbbs-tcp
      port: 31115
      targetPort: 31115
      protocol: TCP
    - name: hbbs-udp
      port: 31116
      targetPort: 31116
      protocol: UDP
    - name: hbbs-tcp-main
      port: 31116
      targetPort: 31116
      protocol: TCP
    - name: hbbr-tcp
      port: 31117
      targetPort: 31117
      protocol: TCP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rustdesk-pvc
spec:
  accessModes:
    - ReadWriteOnce # RWM would be available only with pro version
  resources:
    requests:
      storage: 1Gi
